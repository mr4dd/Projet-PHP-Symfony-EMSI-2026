{% extends 'base.html.twig' %}

{% block title %}Gestion des Modules{% endblock %}

{% block body %}
<style>
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html, body {
    height: 100%;
    font-family: 'Arial', sans-serif;
}

body {
    background-color: #f7f9fc;
    color: #333;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
}

a {
    text-decoration: none;
    color: inherit;
}

.navbar {
    background-color: #343a40;
    color: #fff;
    display: flex;
    justify-content: space-between;
    padding: 1.5rem 2rem;
    align-items: center;
    position: sticky;
    top: 0;
    z-index: 10;
    width: 100%;
}

.navbar .logo a {
    font-size: 1.75rem;
    font-weight: bold;
    color: #fff;
}

.navbar .nav-links {
    display: flex;
    gap: 1.5rem;
    list-style: none;
}

.navbar .nav-links li {
    list-style: none;
}

.navbar .nav-links li button {
    font-size: 1rem;
    background: transparent;
    border: none;
    color: #fff;
    display: flex;
    align-items: center;
    cursor: pointer;
    padding: 0.5rem 1rem;
    border-radius: 5px;
    transition: background-color 0.3s;
}

.navbar .nav-links li button:hover {
    background-color: #f4a261;
}

.navbar .nav-links li button i {
    margin-right: 8px;
}

main {
    display: flex;
    flex-direction: column;
    padding: 2rem;
    flex-grow: 1;
}

.dashboard-overview {
    display: flex;
    gap: 2rem;
    margin-bottom: 2rem;
}

.dashboard-card {
    flex: 1;
    background: #fff;
    padding: 2rem;
    border-radius: 10px;
    box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
    text-align: center;
}

.actions {
    display: flex;
    gap: 1.5rem;
    margin-top: 1.5rem;
}

form {
    background: #fff;
    padding: 2rem;
    border-radius: 10px;
    box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
    margin-bottom: 2rem;
}

form h4 {
    margin-bottom: 1rem;
}

form label {
    font-weight: bold;
    margin-bottom: 0.5rem;
    display: block;
}

form input, form select {
    width: 100%;
    padding: 1rem;
    border-radius: 5px;
    border: 1px solid #ddd;
    margin-bottom: 1rem;
    font-size: 1rem;
    background-color: #f7f9fc;
}

form button {
    background-color: #007bff;
    color: white;
    border: none;
    padding: 1rem 2rem;
    border-radius: 5px;
    cursor: pointer;
    font-size: 1rem;
    transition: background-color 0.3s;
}

form button:hover {
    background-color: #f4a261;
}

.table-container {
    background: #fff;
    padding: 2rem;
    border-radius: 10px;
    box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
    margin-top: 2rem;
}

table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 1rem;
}

table th, table td {
    padding: 1rem;
    text-align: left;
    border-bottom: 1px solid #ddd;
}

table th {
    background-color: #f4f7fc;
    font-weight: bold;
}

table td {
    background-color: #fff;
}

table td button {
    background: transparent;
    border: none;
    cursor: pointer;
    padding: 0.5rem;
    margin: 0 0.25rem;
    font-size: 1rem;
    border-radius: 5px;
    transition: background-color 0.3s;
}

table td button:hover {
    background-color: #f4a261;
}

footer {
    background-color: #343a40;
    color: #fff;
    padding: 1.5rem;
    text-align: center;
}

@media (max-width: 768px) {
    .navbar .nav-links {
        flex-direction: column;
        gap: 1rem;
    }

    .dashboard-overview {
        flex-direction: column;
    }

    .actions {
        flex-direction: column;
    }

    form {
        padding: 1rem;
    }

    .table-container {
        padding: 1rem;
    }
}
</style>

<header>
    <nav class="navbar">
        <div class="logo">
            <a href="/">Gestion Scolaire</a>
        </div>
        <ul class="nav-links">
            <li><button class="btn" onclick="navigateTo('/students')"><i class="fas fa-users"></i> Élèves</button></li>
            <li><button class="btn" onclick="navigateTo('/courses')"><i class="fas fa-chalkboard-teacher"></i> Cours</button></li>
            <li><button class="btn" onclick="navigateTo('/modules')"><i class="fas fa-cogs"></i> Modules</button></li>
            <li><button class="btn" onclick="navigateTo('/profile')"><i class="fas fa-user"></i> Profil</button></li>
            <li><button class="btn" onclick="navigateTo('/logout')"><i class="fas fa-sign-out-alt"></i> Déconnexion</button></li>
        </ul>
    </nav>
</header>

<main>
    <section class="dashboard-overview">
        <div class="dashboard-card">
            <h3>Gestion des Modules</h3>
            <p>Ajoutez ou modifiez des modules en remplissant le formulaire ci-dessous.</p>
        </div>
    </section>

    <section class="form-section">
        <form>
            <h4>Ajouter ou Modifier un Module</h4>

            <label for="module-name">Nom du Module</label>
            <input type="text" id="module-name" placeholder="Ex. Mathématiques Avancées" required>

            <label for="module-description">Description</label>
            <input type="text" id="module-description" placeholder="Ex. Cours avancé en Mathématiques" required>

            <button type="submit">Enregistrer le Module</button>
        </form>
    </section>

    <section class="table-container">
        <h4>Liste des Modules</h4>
        <table>
            <thead>
                <tr>
                    <th>Nom du Module</th>
                    <th>Description</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>Mathématiques Avancées</td>
                    <td>Cours avancé en Mathématiques</td>
                    <td>
                        <button class="btn-edit"><i class="fas fa-edit"></i> Modifier</button>
                        <button class="btn-delete"><i class="fas fa-trash-alt"></i> Supprimer</button>
                    </td>
                </tr>
                <tr>
                    <td>Physique Quantique</td>
                    <td>Introduction à la physique quantique</td>
                    <td>
                        <button class="btn-edit"><i class="fas fa-edit"></i> Modifier</button>
                        <button class="btn-delete"><i class="fas fa-trash-alt"></i> Supprimer</button>
                    </td>
                </tr>
            </tbody>
        </table>
    </section>
</main>

<footer>
    <p>&copy; 2025 Système de Gestion Scolaire | Tous droits réservés</p>
</footer>

<script>
    function navigateTo(path) {
        window.location.href = path;
    }
</script>

{% endblock %}

